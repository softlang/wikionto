PREFIX : <http://myWikiTax.de/>
SELECT DISTINCT  (COUNT(?instance) AS ?number)
WHERE {
    SELECT DISTINCT ?instance WHERE {
        ?instance :instanceOf ?classifier.
        ?root :name "Computer_languages".
        ?classifier :isA* ?root.
        }
}
