PREFIX : <http://myWikiTax.de/>
SELECT DISTINCT ?name
WHERE {
   ?c :name ?name.

   FILTER EXISTS {
      ?c :marked "false".
   }
   FILTER EXISTS {
      ?c :isA* ?r.
      ?r :name "Computer_languages".
   }
   FILTER regex(str(?c), "Classifier").
}
